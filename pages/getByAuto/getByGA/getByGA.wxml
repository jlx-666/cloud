<label >输入难度值：</label>
<input bindinput="setDifficulty" type="double" placeholder="请输入难度" name="difficulty"></input>
<button bindtap="getPaperGA">开始组卷</button>
<button wx:if="{{isloading}}" bindtap="goExercise">开始做题</button>
<button wx:if="{{isloading}}" bindtap="savePaper">上传试题</button>

